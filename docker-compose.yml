version: '2.27'

services:
  enpal:
    image: michaelantonfischer/enpal-modbus
    container_name: enpal-modbus
    ports:
      - "502:502"
    env_file:
      - .env
    networks:
      - enpal-modbus

networks:
  enpal-modbus:
    driver: bridge