version: '3.8'

services:
  enpal:
    image: michaelantonfischer/enpal-modbus
    container_name: enpal-modbus
    ports:
      - "502:502"
    env_file:
      - .env
    networks:
      - enpal-modbus-network

networks:
  enpal-modbus-network:
    driver: bridge